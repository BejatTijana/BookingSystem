@model BookingSystem.ViewModels.AccommodationFormViewModel
@{
    ViewBag.Title = "Edit accommodation";
    bool locked = !Model.IsAvailable;

    var inputAttrs = new Dictionary<string, object> { { "class", "form-input" } };
    if (locked) { inputAttrs["disabled"] = "disabled"; }

    var priceAttrs = new Dictionary<string, object>(inputAttrs)
    {
        { "type", "number" },
        { "step", "0.01" },
        { "min", "0" }
    };
    var guestsAttrs = new Dictionary<string, object>(inputAttrs)
    {
        { "type", "number" },
        { "step", "1" },
        { "min", "1" }
    };
    var textAreaAttrs = new Dictionary<string, object>(inputAttrs)
    {
        { "rows", 4 }
    };

    string imgSrc = string.IsNullOrEmpty(Model.ExistingImage)
        ? Url.Content("~/Content/images/placeholder.jpg")
        : Url.Content("~/Content/images/" + Model.ExistingImage);

    object formAttrs = locked
        ? (object)new { novalidate = "novalidate" }
        : new { enctype = "multipart/form-data", novalidate = "novalidate" };
}

<section class="form-card">
    <h1>Edit accommodation</h1>

    @if (locked)
    {
        <div class="disabled-banner">This accommodation is unavailable. Set it back to Available to edit other fields.</div>
    }

    <img class="current-image-preview"
         src="@imgSrc"
         alt="@Model.Name"
         onerror="this.onerror=null;this.src='@Url.Content("~/Content/images/placeholder.jpg")';" />

    @using (Html.BeginForm("Edit", "HostAccommodations", new { id = Model.Id }, FormMethod.Post, formAttrs))
    {
        @Html.AntiForgeryToken()
        @Html.ValidationSummary(true, "", new { @class = "validation-summary" })
        @Html.HiddenFor(m => m.ExistingImage)

        <div class="form-group">
            @Html.LabelFor(m => m.Name)
            @Html.TextBoxFor(m => m.Name, inputAttrs)
            @Html.ValidationMessageFor(m => m.Name, "", new { @class = "field-error" })
        </div>

        <div class="form-group">
            @Html.LabelFor(m => m.Type)
            @Html.EnumDropDownListFor(m => m.Type, inputAttrs)
            @Html.ValidationMessageFor(m => m.Type, "", new { @class = "field-error" })
        </div>

        <div class="form-group">
            @Html.LabelFor(m => m.Description)
            @Html.TextAreaFor(m => m.Description, textAreaAttrs)
            @Html.ValidationMessageFor(m => m.Description, "", new { @class = "field-error" })
        </div>

        <div class="form-group">
            @Html.LabelFor(m => m.Address)
            @Html.TextBoxFor(m => m.Address, inputAttrs)
            @Html.ValidationMessageFor(m => m.Address, "", new { @class = "field-error" })
        </div>

        <div class="form-group">
            @Html.LabelFor(m => m.City)
            @Html.TextBoxFor(m => m.City, inputAttrs)
            @Html.ValidationMessageFor(m => m.City, "", new { @class = "field-error" })
        </div>

        <div class="form-group">
            @Html.LabelFor(m => m.PricePerNight)
            @Html.TextBoxFor(m => m.PricePerNight, priceAttrs)
            @Html.ValidationMessageFor(m => m.PricePerNight, "", new { @class = "field-error" })
        </div>

        <div class="form-group">
            @Html.LabelFor(m => m.MaxGuests)
            @Html.TextBoxFor(m => m.MaxGuests, guestsAttrs)
            @Html.ValidationMessageFor(m => m.MaxGuests, "", new { @class = "field-error" })
        </div>

        <div class="form-group">
            <label>
                @Html.CheckBoxFor(m => m.IsAvailable)
                Available
            </label>
        </div>

        if (!locked)
        {
            <div class="form-group">
                <label for="imageFile">Replace image (optional, .jpg .jpeg .png, max 2 MB)</label>
                <input type="file" name="imageFile" id="imageFile" accept=".jpg,.jpeg,.png" class="form-input file-input" />
            </div>
        }

        <div class="form-actions">
            <button type="submit" class="btn-primary">Save changes</button>
            <a class="btn-secondary" href="@Url.Action("Index")">Cancel</a>
        </div>
    }
</section>
